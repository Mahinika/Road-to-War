diff --git a/node_modules/playwright-core/lib/server/electron/electron.js b/node_modules/playwright-core/lib/server/electron/electron.js
index 3fb4963..7146ded 100644
--- a/node_modules/playwright-core/lib/server/electron/electron.js
+++ b/node_modules/playwright-core/lib/server/electron/electron.js
@@ -129,7 +129,9 @@ class Electron extends import_instrumentation.SdkObject {
   }
   async launch(progress, options) {
     let app = void 0;
-    let electronArguments = ["--inspect=0", "--remote-debugging-port=0", ...options.args || []];
+    // Use timeout from options, default to 120 seconds for development
+    const launchTimeout = options.timeout || 120000;
+    let electronArguments = [...options.args || []];
     if (import_os.default.platform() === "linux") {
       const runningAsRoot = process.geteuid && process.geteuid() === 0;
       if (runningAsRoot && electronArguments.indexOf("--no-sandbox") === -1)
@@ -191,9 +193,9 @@ class Electron extends import_instrumentation.SdkObject {
         progress.metadata.log
       ].join("\n"));
     });
-    const nodeMatchPromise = waitForLine(progress, launchedProcess, /^Debugger listening on (ws:\/\/.*)$/);
-    const chromeMatchPromise = waitForLine(progress, launchedProcess, /^DevTools listening on (ws:\/\/.*)$/);
-    const debuggerDisconnectPromise = waitForLine(progress, launchedProcess, /Waiting for the debugger to disconnect\.\.\./);
+    const nodeMatchPromise = waitForLine(progress, launchedProcess, /^Debugger listening on (ws:\/\/.*)$/, launchTimeout);
+    const chromeMatchPromise = waitForLine(progress, launchedProcess, /^DevTools listening on (ws:\/\/.*)$/, launchTimeout);
+    const debuggerDisconnectPromise = waitForLine(progress, launchedProcess, /Waiting for the debugger to disconnect\.\.\./, launchTimeout);
     try {
       const nodeMatch = await nodeMatchPromise;
       const nodeTransport = await import_transport.WebSocketTransport.connect(progress, nodeMatch[1]);
@@ -241,7 +243,7 @@ class Electron extends import_instrumentation.SdkObject {
     }
   }
 }
-async function waitForLine(progress, process2, regex) {
+async function waitForLine(progress, process2, regex, timeout) {
   const promise = new import_utils.ManualPromise();
   const rl = readline.createInterface({ input: process2.stderr });
   const failError = new Error("Process failed to launch!");
@@ -258,6 +260,13 @@ async function waitForLine(progress, process2, regex) {
       promise.resolve(match);
   }
   try {
+    // If timeout is provided, wrap with timeout promise
+    if (timeout) {
+      const timeoutPromise = new Promise((_, reject) => {
+        setTimeout(() => reject(new Error(`Timeout ${timeout}ms exceeded while waiting for ${regex}`)), timeout);
+      });
+      return await progress.race(Promise.race([promise, timeoutPromise]));
+    }
     return await progress.race(promise);
   } finally {
     import_eventsHelper.eventsHelper.removeEventListeners(listeners);
