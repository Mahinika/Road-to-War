{
  "outDir": "agent-out",
  "promptsDir": "agent-out/prompts",
  "patchesDir": "agent-out/patches",
  "context": {
    "maxFiles": 8,
    "maxMatchesPerFile": 3,
    "contextLines": 12,
    "includeFiles": ["PROJECT_INDEX.md", "memory-bank/projectbrief.md", "memory-bank/activeContext.md"]
  },
  "lanes": [
    {
      "name": "gameplay",
      "scope": ["src/managers/", "src/generators/", "src/utils/"],
      "instructions": "You are the Gameplay agent. Stay strictly within scope. Follow the Manager Pattern and Event-Driven architecture. No hard-coded values; use JSON data. Output ONLY a unified diff (git patch). If context is insufficient, ask exactly one precise question instead of guessing."
    },
    {
      "name": "ui",
      "scope": ["src/scenes/", "public/"],
      "instructions": "You are the UI agent. Stay strictly within scope. ALWAYS use wow-ui-helper.js and follow the WoW WotLK aesthetic defined in ui-theme.js. Output ONLY a unified diff (git patch). If context is insufficient, ask exactly one precise question instead of guessing."
    },
    {
      "name": "infra",
      "scope": ["electron/", "scripts/", "tools/", "vite.config.js", "package.json", "eslint.config.js"],
      "instructions": "You are the Infra/QA agent. Stay strictly within scope. Maintain the Electron IPC bridge pattern and ensure logs are correctly redirected. Output ONLY a unified diff (git patch). If context is insufficient, ask exactly one precise question instead of guessing."
    }
  ]
}




